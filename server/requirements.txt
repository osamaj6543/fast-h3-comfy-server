# Runtime dependencies for the FastH3 Comfy Server.
#
# uv workflow:
#   uv venv                              # create the environment
#   uv pip sync requirements.lock        # install the pinned production set
#   uv pip sync requirements-dev.lock    # ...or the dev/test set
#
# Lockfiles are regenerated from these files with:
#   uv pip compile --python-platform x86_64-unknown-linux-gnu \
#       --python-version 3.11 --generate-hashes \
#       requirements.txt -o requirements.lock

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
websockets>=12.0
python-multipart>=0.0.9

